feat(skills,approvals): P6 - skill integrity + risk-tiered approval inbox

- `birkin skills validate` (birkin/skills/validate.py): lints SKILL.md
  frontmatter (errors on missing name/description, warns on missing
  version/license/When-to-Use); py_compile bundled Python scripts. New
  CLI subcommand, exits non-zero on any error. Smoke-run against the
  bundled catalog: 48/48 clean.
- birkin/risk.py: per-category risk tier (memory/skill=low, cron=medium,
  shell=high; unknown defaults to medium - fail-safe). `birkin review`
  and /api/approvals order pending items highest-risk-first and tag
  each line with its tier. Display-only; auto-approval still governed
  by config["auto_approve"].
- 14 new tests (test_skill_integrity.py): clean/missing-field/broken-
  script/missing-When-to-Use; risk-for known + unknown categories;
  sort_by_risk; review_cli ordering. Full suite: 199 passed,
  coverage 79.95% (>=75%).
- Docs: STATUS H6 section, ADR-022.

Closes hardening P6. P2-P6 of HARDENING-PLAN.md complete.
