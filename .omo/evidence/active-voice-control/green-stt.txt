SCENARIO: OpenAI STT and microphone collection GREEN

EXACT COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_openai_voice.py -q

ASSERTION RESULT:
7 passed

PROCESS RESULT:
exit 1 only because the pre-existing pytest defaults apply the 75% full-project
coverage gate to targeted invocations.

CLEAN TARGETED COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_openai_voice.py -q --no-cov

CLEAN TARGETED RESULT:
exit 0
7 passed

PROVEN CONTRACTS:
- recorded WAV uploaded with model `gpt-transcribe`;
- in-memory AudioData encoded as mono PCM16 WAV before upload;
- transcript text trimmed and returned;
- microphone capture requests mono float32, exact bounded frames, and waits;
- recorder seam prevents hardware access in CI.

DIAGNOSTICS AND SIZE:
audio.py: zero diagnostics, 173 lines.
openai_voice.py: zero diagnostics, 150 lines.
config.py: zero diagnostics, 113 lines.
controller.py: zero diagnostics, 239 lines.
cli.py: zero diagnostics.
test_openai_voice.py: zero diagnostics.
