SCENARIO: S3 background broker and safety GREEN

EXACT COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_background_broker.py tests/test_voice_security.py -q

ASSERTION RESULT:
3 passed

PROCESS RESULT:
exit 1 only because the pre-existing pytest defaults require 75% whole-project
coverage from every targeted invocation.

CLEAN TARGETED COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_background_broker.py tests/test_voice_security.py tests/test_gateway_voice.py -q --no-cov

CLEAN TARGETED RESULT:
exit 0
6 passed

PROVEN CONTRACTS:
- max_workers=1 prevents a queued job from starting while the first is blocked.
- events are ordered queued -> running -> progress -> succeeded.
- every transition is atomically reflected in the JSON receipt.
- queued cancellation prevents the callable from executing.
- destructive `Gateway.handle("voice", ...)` keeps both approval flags false.
- broker close joins all worker threads.

DIAGNOSTICS:
birkin/background.py: no diagnostics, 246 lines.
birkin/voice/mission.py: no diagnostics, 50 lines.
birkin/cli.py: no diagnostics.
