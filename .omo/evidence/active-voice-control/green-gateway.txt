SCENARIO: S2 Gateway and TTS contract GREEN

EXACT COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_gateway_voice.py -q

ASSERTION RESULT:
14 passed

PROCESS RESULT:
exit 1 only because the repository's pre-existing pytest defaults require 75%
full-project coverage from every targeted invocation.

CLEAN TARGETED COMMAND:
uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_gateway_voice.py tests/test_voice_wake.py -q --no-cov

CLEAN TARGETED RESULT:
exit 0
19 passed

PROVEN CONTRACTS:
- LocalHTTPChannel preserves explicit `channel=voice`.
- `telegram`/unknown trust-channel spoof is rejected with HTTP 400.
- GatewayClient sends fixed session and command through POST /message.
- OpenAITTS requests gpt-4o-mini-tts/coral/PCM and streams bytes.
- PcmFileSink receives the exact response bytes.
- Every ephemeral HTTP thread stops and joins.

ENVIRONMENT CORRECTION:
`uv run pytest` had previously selected the global pytest executable because
the project only exposed an optional `dev` extra. A default uv `dev` dependency
group now makes the literal command use the worktree venv and source tree.

DIAGNOSTICS:
All new/changed voice adapter and test files have zero diagnostics.
`local_http.py` has zero diagnostics.

FINAL HARDENING:
- `GatewayClient` accepts only exact loopback HTTP `/message` endpoints and
  uses direct `HTTPConnection` transport.
- HTTP status >= 400 becomes `GatewayVoiceError`.
- Local HTTP enforces a 1 MB cap, a 2-second body-read timeout, complete bodies,
  string text/channel fields, and the `http|voice` allowlist.
