SCENARIO: S2 Gateway and TTS contract RED
COMMAND: uv --directory "C:/Users/lg/Documents/Claude/Projects/Birkin/active-voice-control" run pytest tests/test_gateway_voice.py -q --no-cov
EXIT: 1
RESULT: 3 failed for the intended missing behavior

1. Explicit `channel=voice` reached the fake Gateway as `http`.
2. `channel=telegram` spoof returned success instead of HTTP 400.
3. The voice Gateway/TTS adapter contract reported:
   `voice gateway and TTS contract is not implemented`.

Every ephemeral LocalHTTPChannel was started on a daemon thread only after its
ready event, stopped in `finally`, joined with a bounded timeout, and confirmed
dead. No server, thread, or bound port remained.

INVALID PRE-RED ATTEMPTS:
- A direct blocking call to `LocalHTTPChannel.start()` caused a 120-second hang.
- A repeated 60-second run and bounded faulthandler reproductions confirmed the
  test fixture had violated the channel's blocking lifecycle contract.
- The fixture was corrected to mirror `tests/test_gateway.py`; no production
  framing workaround remains.
