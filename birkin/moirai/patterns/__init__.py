"""Bundled workflow patterns — each one is a working script and a worked example.

`birkin moirai run <name>` finds these by name after the user's own scripts
directory, so a user file of the same name shadows the bundled one.
"""
