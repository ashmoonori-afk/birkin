# Runtime data & secrets
.birkin/
*.log
.env
.omc/
# Credential files. These normally live in $BIRKIN_HOME or ~/.codex, but a
# stray copy inside the repo must never become committable — the audit found
# only .env was covered.
codex-auth.json
auth.json
.credentials.json
secrets.json
*.pem
id_rsa*
# Legacy local artifacts (old birkin app)
birkin_config.json
*.db
*.db-shm
*.db-wal
*.exe
/memory/

# Internal planning (not shipped): productization analysis, dev roadmap, goal prompt
docs/productization/
.plans/

# One-shot local helper scripts — embed a personal Telegram chat id; never ship
/fix_birkin_config.py
# Stray packaging artifacts inside the skills tree
skills/**/*.zip

# Local benchmark workspace. Already-committed reproducibility scripts remain
# tracked; new harnesses, private fixtures, and generated results stay local.
benchmarks/
docs/paper/
# hermes-agent re-comparison research note (local only, 2026-07-23)
docs/hermes-comparison-20260723.md
# TUI redesign plan (local research note, 2026-07-24)
docs/tui-redesign-plan.md
docs/dev-plan-20260725.md
docs/moirai-design.md
# Superseded Morpheus implementation notes; current behavior lives in ADRs/code
docs/morpheus-skillification-plan.md
docs/morpheus-skillification.md
# Working evidence/drafts dir (codex receipts, model-test logs) — local only.
# The native application contracts are the sole reviewed, tracked exception.
.omo/*
!.omo/plans/
.omo/plans/*
!.omo/plans/native-app/
!.omo/plans/native-app/**
!.omo/plans/native-windows-app/
!.omo/plans/native-windows-app/**
# Generated local research, reports, and one-shot QA scratch files
/reports/
/research-*.md
/*-analysis-*.md
/ulw-*.md
/.tmp-*
# One-shot generated UI handoff artifacts
/DESIGN.md
/*.html
# Tests that load the local-only benchmark harness (benchmarks/ is ignored)
tests/test_bench_curation200.py
tests/test_bench_lme_e2e.py

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
# Legacy build artifacts (old birkin app)
*.spec
/traces/

# OS
.DS_Store
Thumbs.db
uv.lock
